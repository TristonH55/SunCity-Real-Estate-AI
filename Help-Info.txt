#Access PostgreSQL as admin
sudo -u postgres psql
⛔ Important
Once inside psql:
Commands end with a semicolon ;

CREATE DB USER suncity_admin WITH PASSWORD 'SunCity1234#';

node -e "require('bcryptjs').hash('SunCity1234#', 12).then(console.log)"